# Base URL of the API server (see server/). Defaults to http://localhost:3001
# if unset, so this only needs to be set when running the server on a
# different host/port.
VITE_API_BASE_URL=http://localhost:3001
